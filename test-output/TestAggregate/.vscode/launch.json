{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to TestAggregate",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}",
            "postDebugTask": "kill debug" 
        }
    ]
}