{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to _ReplaceMe_",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}